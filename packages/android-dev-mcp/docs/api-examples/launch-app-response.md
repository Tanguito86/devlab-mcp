# Launch App Response

Tool:

```json
{ "tool": "android_launch_app", "args": { "app": "sampleApp", "deviceId": "SERIAL" } }
```

Example response:

```text
Launched sampleApp (com.example.myapp/.MainActivity)
Starting: Intent { cmp=com.example.myapp/.MainActivity }
```
