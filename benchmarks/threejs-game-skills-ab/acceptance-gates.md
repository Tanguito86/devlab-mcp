# Acceptance gates — generated

<!-- GENERATED FROM benchmark-contract.json (ab04-v2). DO NOT EDIT. -->

- Contract: version `ab04-v2`; canonical UTF-8/LF SHA-256 verified before both legs.
- Pair isolation: one fresh independent context and directory per leg; no cross-leg outcomes or artifacts.
- Model equality: `same-exact-model-and-build-within-pair`; reasoning effort `ultra`.
- Shared seed: `424242`.
- Shared scaffold: `devlab-internal-threejs-game-benchmark-v1` with tree hash from the contract.
- Runtime: `Three.js WebGPURenderer`, `native-webgpu`, full verified Chromium and `NVIDIA_RTX_2060_TURING_HARDWARE`.
- Viewports: desktop `1280×720`; mobile `390×844`.
- Budget: `240` active minutes total per leg, `1` builder run, `1` implementation cycle, `2` correction cycles and `3` total passes.
- Repetitions: `2` frozen captures per state, `10` bot playtests per leg and `3` performance repetitions for each of `idle`, `encounter-normal`, `stress`, `boss`, `mobile`.
- Security: no global install, external script/scaffold, paid API, non-loopback request, copied upstream file or credential.
- Correctness: frozen install, build and typecheck pass; console/page/network errors zero; start, checkpoint, pause, defeat, restart, restoration, mini-boss and victory complete.
- Determinism: repeated frozen PNG and RGBA bytes exact; controlled change detected; live gameplay compared statistically.
- Runtime lifecycle: bounded resources, resize and desktop/mobile input pass, device-loss recovery pass, software adapter rejected.
- Evaluation: complete anonymized evidence, blinded scoring and documented decision.
