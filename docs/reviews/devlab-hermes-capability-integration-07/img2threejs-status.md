# img2threejs status

img2threejs is registered as `PILOT_REQUIRED / NOT_INSTALLED`, pinned at `b604139f51d6831780240e8cf1d8b21a42401d0a` (Apache-2.0).

Required blockers are TypeScript interpolation sanitization, bounds for all five PNG parsers, and independent visual-gate hardening. `ground-blade` is rejected until its winding fix; PMREM needs an adapter; classic materials are portable through a node library; presentation runtime needs a DevLab adapter.

`DEVLAB-IMG2THREEJS-ASSET-PILOT-01` is authorized only after those fixes.
