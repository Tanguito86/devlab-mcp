# Promotion records

The candidate record binds asset identity, SemVer, `PILOT -> CANDIDATE`, content-manifest SHA-256, sorted critic reports, decision and policy version. It contains no wall-clock field and includes a self-hash. It is evidence only: Cinder remains `PILOT` until a separately authorized promotion applies the transition.
