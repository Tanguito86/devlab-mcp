# glTF round-trip

The read-only GLB import preserves the essential hierarchy, 10 meshes, 5 materials, 2,180 base triangles, canonical names and bounds within `1e-5`. The imported scene has one loader-owned wrapper node in addition to the 14 exported nodes. Every round-trip check is PASS.
