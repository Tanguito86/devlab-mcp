# Asset catalog

`assets/catalog/asset-catalog.json` is schema-closed, canonically ordered and versioned by `assetId + version`. It contains one entry, `cinder-relay-drone@1.0.0-pilot.1`, with relative paths, SHA-256 provenance, budget and critic profiles, WebGL target, and consumer-neutral export pointers. The catalog references manifests rather than duplicating them.
