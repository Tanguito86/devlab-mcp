# Decision summary

Adopt the renderer-free mechanisms and executable contracts; keep content and presentation in consumers. The implementation is one zero-runtime-dependency workspace package. Pool capacity never defines encounter pressure. Camera transforms are injected. Device loss is infrastructure-only. Human evidence cannot be inferred from bots.
