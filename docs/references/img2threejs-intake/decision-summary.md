# Decision summary

Register img2threejs as an `AssetFactoryAdapter` candidate at pin `b604139f51d6831780240e8cf1d8b21a42401d0a`. Before any pilot: sanitize emitted TypeScript interpolations, bound all five PNG parsers, and harden independent visual gates. `ground-blade` is rejected until winding is fixed; PMREM and presentation require DevLab adapters.
